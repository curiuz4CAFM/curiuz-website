# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
/build
next-env.d.ts

# production / vercel
/.vercel
*.tsbuildinfo

# env files (keep .env.local.example tracked)
.env
.env*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# os / editor
.DS_Store
*.pem
Thumbs.db

# large source assets kept locally, not needed at runtime
# (the versions the site actually uses live in /public)
/lib/*.mp4
/lib/Curiuz Technologies Logo Concepts/
